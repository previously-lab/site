---
title: 召回
description: 情景回忆同事——你用自然语言提问,它自己在记忆里检索(时间线窗口、Strand、摘要,再到配额受限的全文深读),用挂着原文引用的 references、检索轨迹 searched 和置信度回答。
order: 4
---

# 回忆(Recall)

回忆是 Previously 检索相关历史对话的方式。在 v1.0 里,它不再是主 Agent 驱动的检索工具,而是一位**同事**:一位情景回忆专家,主 Agent 用自然语言向它提问,它自己搜记忆,然后带回一份挂着原文引用的回答。每一步都实时渲染在聊天中:回忆在明处运行,而不是藏在 spinner 后面。

## 是同事,不是工具

契约的两端都是对话式的。主 Agent 用一个朴素的问题调用 `recall`——`{question: "我们之前有没有和用户聊过婚礼致辞?"}`——就像你问一位当时在场的同事。Recall 也用自然语言回答。

两条纪律贯穿整个交互:

1. **同事关系,用户永远是第三方。** 主 Agent 与 recall 互以同事相称,称你为「用户」——任何一方都不会假扮你,也不会把对方当成你。这写进了共享的子 Agent 基础提示词和主 Agent 的工具描述里。
2. **证据锚定。** 凡是对情景的断言,必须挂原文引用或 slice 指针;引不出原文的断言标为不确定。回忆、评分、方向变更全部遵守——幻觉是被结构性地锁死的,而不是指望模型自觉。

<Callout variant="info">
**要点**:主 Agent 不再亲自在记忆里导航。它问一位熟悉档案的同事,然后采信——并可以核验——对方的回答。
</Callout>

## 回忆报告

Recall 的回答落地为一份结构化报告,四个部分:

| 字段 | 内容 |
|-------|-----------------|
| `answer` | 自然语言回答,用你的语言。「我们不记得聊过这个」是合法且重要的答案;硬凑命中是被禁止的。 |
| `references[]` | `{slice_id, quote, note}`——answer 里每个情景断言都必须在这里挂一条原文引用。 |
| `searched[]` | 实际找过的路径——时间窗、追过的 Strand、读过的切片——供主 Agent 判断这次回忆有多完整。 |
| `confidence` | 0–1。 |

`searched` 轨迹让否定答案也可信:同样是「我们不记得」,查过相关时间窗和两条 Strand 之后说的,与扫了一眼时间线就放弃的,含义完全不同。

## Recall 如何检索

Recall 持有全套只读记忆工具——`readGlobalTimeline`、`readTimelineWindow`、`readStrand`、`listStrands`、`readSliceSummary`,以及 `readSlice`(含 range 协议)——按人回忆的方式工作:先广度,深度只花在线索指向的地方。

1. **时间锚点优先。** 问题里带时间锚点(「上周」、「婚礼前一天」)时,先用 `readTimelineWindow` 锁定物理时间窗。
2. **追踪 Strand。** 从问题里的线索出发,沿匹配的 Strand——关键词到切片的索引——找到所有携带该话题的切片。
3. **先摘要,后全文。** 切片摘要用来判断值不值得打开;全文深读配额**最多 5 片**,深度只花在重要的地方。
4. **找不到就直说。** 检索确实一无所获时,诚实的回答是「我们想不起来这个」,并附上 `searched` 清单。Recall 绝不硬凑命中。

预算宽到足够认真检索——20 步、240 秒墙钟、温度 0.3(情景理解需要一点温度;诚实由证据锚定的结构保证)。超时不会白跑:recall 边想边写答案,runner 会返回已积累的部分结果。

## 返工信号

主 Agent 只保留了一个记忆读取工具:`readSlice`,专门用于**核验 recall 的回答**——「只在需要核对 recall 说了什么时,才打开原始切片」。

这条核验通道是埋了点的。当主 Agent 调用 recall 之后又自己重读同一个切片,系统会给这次读取分类——快速核对是 `verify`,把 recall 的活重干一遍是 `rework`,同一个问题再问一遍是 `repeat`——并记进 agent 时间线与 fitness store(`src/lib/episodic/rework-signal.ts`)。一次 rework 是对 recall 的隐式扣分:它意味着回答没被采信。这是 fitness 评分最可靠的隐式信号源;攒够了,就会触发 recall playbook 的一次进化(见[用户卡片](/docs/user-card#进化回路))。

<Callout variant="warning">
**回忆一次,然后采信。** 如果 recall 找过且没找到,那就是确定性的答案——主 Agent 不会为同一个问题再问一遍,基于当前对话作答就是正确做法。把同事已经找过的东西再搜一遍,读起来就是不信任,而系统现在恰恰在给这种行为记分。
</Callout>

在演示数据集（[`you`](https://github.com/previously-lab/you)，97 个切片）上跑一次真实的回忆——只有预设问题，带原文引用和检索轨迹实时作答：

<Playground preset="recall-mom" />

## 三阶段聊天渲染

单条助手消息渲染为三个视觉上不同的阶段,在 `ChatMessage` 中按 part 类型划分:

```
推理(大脑图标,可折叠卡片,在气泡内)
    |
    v
回忆 / 工具(历史/工具图标,内联卡片)
    |
    v
响应(文本 + 行内工具调用,在气泡内)
```

### 1. 推理阶段

当主模型启用了思考功能(默认:开启,推理程度按工具栏选择),推理作为 `reasoning` parts 流出。客户端将其渲染为 `ThinkingSteps`——一个带 Brain 图标的 `PhaseIndicator` 可折叠卡片。

- **折叠摘要**:「思考 · Ns」计时器
- **展开内容**:推理 markdown

### 2. 回忆 / 工具阶段

工具调用通过 `ToolRenderer` 以内联可折叠卡片渲染。每个工具——`recall`、`readSlice`、`readPreviously`、`webSearch`、`thinkDeep`——都会得到内容感知的标签,而不是原始工具名:回忆卡片带上提问,切片卡片指明日期(显示「查看了 7 月 16 日的对话」,而不是 "readSlice")。recall 工作时,它的步骤实时流入卡片——读时间窗、追 Strand、打开切片——报告落地时定格为答案与置信度。

每张卡片有五种视觉状态:**运行中**(spinner)、**已完成**(圆点)、**错误**(红)、**中断**(黄)、**拒绝**(红)。点击展开查看完整工具输出——CSS `grid-template-rows` 过渡,无需 JS 测高。

### 3. 响应阶段

响应正文在 `Bubble` 内渲染。文本通过 `MarkdownRenderer`(react-markdown + remark-gfm + rehype-highlight)处理。工具调用按照主模型调用的顺序以内联方式出现,每个由对应的工具渲染器包装。

## 相关文档

- [切片(Slices)](/docs/slices) —— 切片如何创建、关闭和索引
- [主题索引(Strands)](/docs/strands) —— recall 所追踪的语义关键词索引
- [用户卡片](/docs/user-card) —— 当 fitness 分数开口时,进化 recall playbook 的那条回路
- [架构(Architecture)](/docs/architecture) —— 同事制与主 Agent 收窄的工具面
